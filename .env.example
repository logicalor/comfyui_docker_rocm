# ComfyUI Directory Mappings
# Copy this file to .env and customize the paths for your system

# Output directory (generated images)
COMFYUI_OUTPUT=/path/to/comfyui/output

# Models directory (model files, LoRAs, VAEs, checkpoints, etc.)
COMFYUI_MODELS=/path/to/comfyui/models

# Custom nodes directory (community plugins)
COMFYUI_CUSTOM_NODES=/path/to/comfyui/custom_nodes

# Logs directory
COMFYUI_LOGS=/path/to/comfyui/logs

# Cache directory
COMFYUI_CACHE=/path/to/comfyui/cache

# User Default directory (workflows, settings)
COMFYUI_USER_DEFAULT=/path/to/comfyui/user/default

# =============================================================================
# ComfyUI Runtime Options
# =============================================================================
# Add any command-line arguments for main.py here
# Examples:
#   --lowvram              Enable low VRAM mode
#   --highvram             Enable high VRAM mode  
#   --normalvram           Default VRAM mode
#   --novram               Use CPU for everything (very slow)
#   --cpu                  Use CPU only
#   --disable-smart-memory Disable smart memory management
#   --reserve-vram 1.0     Reserve VRAM in GB for other apps (e.g., 1.0 = 1GB)
#   --fast                 Enable faster inference (may use more memory)
#   --dont-print-server    Suppress server startup messages
#   --preview-method auto  Preview method: auto, latent2rgb, taesd
#   --use-split-cross-attention  Use split cross attention (saves VRAM)
#   --disable-xformers     Disable xformers
#   --force-fp16           Force FP16 precision
#   --force-fp32           Force FP32 precision
#   --bf16-unet            Use BF16 for UNet
#   --fp8_e4m3fn-unet      Use FP8 for UNet
#   --port 8188            Web interface port (default: 8188)
#
# Multiple options can be combined, space-separated:
COMFYUI_EXTRA_ARGS=
